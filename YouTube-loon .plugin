#!name= 𝐘𝐨𝐮𝐓𝐮𝐛𝐞🚫/⿻/▶︎ɢʀ版
#!desc= Youtube & Youtube Music 去广告/画中画/后台播放
#!author= deezertidal
#!icon= https://upload.wikimedia.org/wikipedia/commons/4/42/YouTube_icon_%282013-2017%29.png
#!homepage=
#!tag= YouTube去广告
#!system= iOS
#!system_version= 15.0
#!loon_version= 3.2.1(372)

#!arguments=blockUpload:true,blockImmersive:true,captionLang:zh-Hans,lyricLang:zh-Hans,engine:auto,debug:false
#!arguments-desc=- 屏蔽参数：[true, false]\n- 翻译参数：[语言代码, off]\n- 脚本引擎：[auto, jsc, webview]\n\n1. 语言代码遵循 Google Translate Languages Codes，当填入 off 时关闭翻译\n2. 推荐使用 jsc 引擎，但您仍可以切换成 webview 体验 Surge/Loon 新特性\n3. 开启调试模式用于输出更多日志

[Argument]
blockUpload = switch, true, tag=blockUpload, desc=是否屏蔽 YouTube 上传按钮
blockImmersive = switch, true, tag=blockImmersive, desc=是否屏蔽 YouTube 选段按钮
captionLang = input, "zh-Hans", tag=captionLang, desc=设置字幕翻译的目标语言
lyricLang = input, "zh-Hans", tag=lyricLang, desc=设置歌词翻译的目标语言
engine = select, "auto", "jsc", "webview", tag=engine, desc=选择脚本执行引擎
debug = switch, false, tag=debug, desc=是否启用调试模式以输出更多日志

[Rule]
# 1. 屏蔽 UDP 协议下的 googlevideo.com 请求
AND,((DOMAIN-SUFFIX,googlevideo.com),(PROTOCOL,UDP)),REJECT
# 2. 屏蔽 UDP 协议下的 youtubei.googleapis.com 请求
AND,((DOMAIN-SUFFIX,youtubei.googleapis.com),(PROTOCOL,UDP)),REJECT

[URL Rewrite]
# 将匹配到的 googlevideo.com initplayback 请求返回占位空白图片，以避免广告显示问题
^https?:\/\/[\w-]+\.googlevideo\.com\/initplayback.+&oad _ REJECT-IMG

[mock-response-body]
# 对匹配到的广告请求返回空白数据，避免广告界面显示异常
^https?:\/\/[\w-]+\.googlevideo\.com\/initplayback.+&oad mock-response-body data-type=text status-code=502

[Script]
# 处理 YouTube 响应，屏蔽上传按钮、选段按钮和翻译功能
# 参数说明：
#   - lyricLang: 歌词翻译语言
#   - captionLang: 字幕翻译语言
#   - blockUpload: 是否屏蔽上传按钮（true/false）
#   - blockImmersive: 是否屏蔽选段按钮（true/false）
#   - debug: 是否启用调试模式（true/false）
http-response ^https:\/\/youtubei\.googleapis\.com\/youtubei\/v1\/(browse|next|player|search|reel\/reel_watch_sequence|guide|account\/get_setting|get_watch) script-path=https://raw.githubusercontent.com/guren777/Shadowrocket/refs/heads/main/youtube.response.js,requires-body=true,binary-body-mode=true,timeout=10,tag=YouTube_Response_Modification,argument={"lyricLang":"{歌词翻译语言}","captionLang":"{字幕翻译语言}","blockUpload":{屏蔽上传按钮},"blockImmersive":{屏蔽选段按钮},"debug":{启用调试模式}}

[MITM]
hostname = %APPEND% *.googlevideo.com, youtubei.googleapis.com